// import { MysqlHelper as sut } from './mysql-helper'

describe('Mysql Helper', () => {
  test('Sould connect if mysql is down', async () => {
    // somente para passar o commit
    expect(1).toBe(1)
  })
})
