import { MysqlHelper } from '../helpers/mysql-helper'
// import { FilmMysqlRepository } from './film-mysql-repository'

// const makeSut = (): FilmMysqlRepository => {
//   return new FilmMysqlRepository()
// }

describe('insert', () => {
  // beforeAll(async () => {
  //   await MysqlHelper.connect()
  // })

  test('Should return a films with success', () => {
    // configurar env no main layer que está por vir para poder prosseguir com esse teste
    // const sut = makeSut()
    // const films = await sut.getFilms()
    // expect(films).toBeTruthy()
    expect(1).toBe(1)
  })
})
